# netlify.toml — na raiz do repo
# O build base já está configurado na UI do Netlify como modules/discover
# Este ficheiro só configura as functions e redirects

[functions]
  directory = "netlify/functions"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
